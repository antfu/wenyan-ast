export interface RenderOptions {
  plotResult: boolean
}

export function render(title: string, code: string, options: Partial<RenderOptions> = {}): string[] {
  // TODO:
  return []
}

export function unrender(files: string[]) {
  // TODO:
}
